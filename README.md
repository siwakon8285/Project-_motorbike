# ระบบจัดการศูนย์ซ่อมรถจักรยานยนต์ (Motorbike Service Management System)

เว็บแอปพลิเคชันครบวงจรสำหรับบริหารจัดการศูนย์ซ่อมรถจักรยานยนต์ รองรับการจองคิวออนไลน์, จัดการสต็อกอะไหล่, ติดตามสถานะงานซ่อม, และดูรายงานสรุปยอดขาย

## ✨ ฟีเจอร์หลัก

### 1. 📅 ระบบจองคิวออนไลน์ (Online Booking)
- ลูกค้าสามารถจองคิวซ่อมล่วงหน้าได้
- เลือกบริการที่ต้องการและระบุอาการเบื้องต้น
- เลือกวันและเวลาที่สะดวก
- ติดตามสถานะการจอง (รอยืนยัน, กำลังดำเนินการ, เสร็จสิ้น)

### 2. 🛠️ บริการและราคา (Services)
- แสดงรายการบริการพร้อมราคามาตรฐาน
- แบ่งหมวดหมู่บริการชัดเจน (เครื่องยนต์, เบรก, ยาง, ถ่ายน้ำมันเครื่อง ฯลฯ)
- ผู้ดูแลระบบสามารถ เพิ่ม/ลบ/แก้ไข รายการบริการได้

### 3. 📦 ระบบจัดการอะไหล่ (Inventory)
- จัดการข้อมูลอะไหล่ในคลัง
- ตัดสต็อกอัตโนมัติเมื่อมีการเบิกใช้
- แจ้งเตือนเมื่อสินค้าใกล้หมด (Low Stock Alert)
- บันทึกประวัติการใช้อะไหล่ในแต่ละงานซ่อม

### 4. 📊 แดชบอร์ดและรายงาน (Dashboard)
- สรุปยอดรายได้ (รายวัน, รายเดือน, ทั้งหมด)
- สถิติการจองและจำนวนลูกค้า
- วิเคราะห์บริการยอดนิยม
- กราฟแสดงแนวโน้มรายได้

### 5. 🔔 ระบบแจ้งเตือน (Notifications)
- แจ้งเตือนเมื่อสถานะการจองเปลี่ยนแปลง
- แจ้งเตือนเมื่อรถซ่อมเสร็จพร้อมรับ
- แจ้งเตือนการยืนยันคิว

### 6. 👤 ระบบสมาชิกและสิทธิ์การใช้งาน (Authentication & Roles)
- **Customer**: จองคิว, ดูประวัติการซ่อม, แก้ไขข้อมูลส่วนตัว
- **Mechanic**: ดูรายการงานซ่อม, อัปเดตสถานะงาน, เบิกอะไหล่
- **Admin**: จัดการทุกส่วนของระบบ, ดูรายงาน, จัดการผู้ใช้งาน

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

### Backend
- **Node.js** & **Express**: เฟรมเวิร์กสำหรับสร้าง API
- **PostgreSQL**: ฐานข้อมูลหลักสำหรับจัดเก็บข้อมูล
- **Redis**: (Optional) สำหรับ Caching หรือ Session
- **JWT**: ระบบยืนยันตัวตน (Authentication)
- **Sequelize/PG**: เชื่อมต่อฐานข้อมูล

### Frontend
- **Next.js 14**: React Framework ประสิทธิภาพสูง
- **TypeScript**: เพื่อความถูกต้องและง่ายต่อการพัฒนา
- **Tailwind CSS**: ตกแต่งหน้าตาเว็บไซต์ให้สวยงามและ Responsive
- **Lucide React**: ชุดไอคอนที่สวยงาม
- **Recharts**: สำหรับแสดงกราฟและข้อมูลสถิติ

## 📂 โครงสร้างโปรเจกต์

```
motorbike_project/
├── backend/                # ส่วนของ Server API
│   ├── config/            # การตั้งค่า Database
│   ├── middleware/        # Authentication & Validation
│   ├── routes/            # API Endpoints
│   └── server.js          # Entry point ของ Backend
├── frontend/               # ส่วนของหน้าเว็บ (Next.js)
│   ├── src/
│   │   ├── app/           # หน้าเว็บต่างๆ (Pages)
│   │   ├── components/    # Components ที่ใช้ซ้ำ
│   │   └── contexts/      # Global State (Auth)
│   └── package.json
├── init_db.js             # สคริปต์สำหรับสร้างตารางในฐานข้อมูลเริ่มต้น
├── package.json           # Root dependencies และ scripts
└── README.md              # เอกสารอธิบายโปรเจกต์
```

## 🚀 การติดตั้งและเริ่มต้นใช้งาน

1. **ติดตั้ง Dependencies**
   ```bash
   # ที่ Root Directory
   npm install

   # เข้าไปที่โฟลเดอร์ frontend และติดตั้ง
   cd frontend
   npm install
   ```

2. **ตั้งค่า Environment Variables**
   - สร้างไฟล์ `.env` ในโฟลเดอร์ `backend` และ `frontend`
   - กำหนดค่าการเชื่อมต่อฐานข้อมูล (PostgreSQL Connection String)

3. **เตรียมฐานข้อมูล**
   ```bash
   # รันสคริปต์เพื่อสร้างตาราง (ตรวจสอบ config db ก่อนรัน)
   node init_db.js
   ```

4. **รันโปรเจกต์**
   ```bash
   # กลับมาที่ Root Directory
   # รันทั้ง Backend และ Frontend พร้อมกัน
   npm run dev
   ```
   - **Frontend**: http://localhost:3000
   - **Backend**: http://localhost:5000
