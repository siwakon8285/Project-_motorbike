# ระบบจัดการศูนย์ซ่อมรถจักรยานยนต์ (Motorbike Service Management System)

ระบบบริหารจัดการศูนย์ซ่อมรถจักรยานยนต์ครบวงจร (Web Application) ที่เชื่อมต่อการทำงานระหว่างลูกค้า ช่างซ่อม และผู้ดูแลระบบ รองรับการจองคิวออนไลน์ การประเมินราคา การจัดการสต็อกอะไหล่แบบ Real-time และระบบผู้ช่วย AI อัจฉริยะ

## 🌟 ฟีเจอร์หลัก (Key Features)

### 👤 สำหรับลูกค้า (Customer)
- **ระบบสมาชิก**: สมัครสมาชิกและเข้าสู่ระบบเพื่อใช้งาน
- **AI Chatbot Assistant**: ผู้ช่วยอัจฉริยะ (น้องช่าง MotoService) คอยตอบคำถามราคาอะไหล่และให้คำปรึกษาอาการรถเสียเบื้องต้นตลอด 24 ชม.
- **จองคิวซ่อม**: เลือกวันและเวลาที่สะดวกสำหรับการนำรถเข้าซ่อม
- **ประเมินราคา (Estimate)**: ตรวจสอบราคาอะไหล่และค่าบริการเบื้องต้น
- **ประวัติการซ่อม**: ดูประวัติการเข้ารับบริการและสถานะการจองปัจจุบัน
- **Dashboard**: หน้าสรุปข้อมูลส่วนตัวและการแจ้งเตือนนัดหมาย

### 🔧 สำหรับช่างซ่อม (Mechanic)
- **จัดการงานซ่อม**: ดูรายการคิวงานและอัปเดตสถานะงานซ่อม (รอซ่อม, กำลังซ่อม, เสร็จสิ้น)
- **ตรวจสอบอะไหล่**: เช็คสต็อกอะไหล่คงเหลือเพื่อวางแผนการซ่อม

### 👨‍💼 สำหรับผู้ดูแลระบบ (Admin)
- **Dashboard ภาพรวม**: ดูกราฟสรุปรายได้, จำนวนลูกค้า, และสถิติการให้บริการ
- **จัดการอะไหล่ (Parts Management)**:
  - เพิ่ม/ลบ/แก้ไข ข้อมูลอะไหล่
  - อัปโหลดรูปภาพอะไหล่
  - **Real-time Sync**: ข้อมูลอะไหล่เชื่อมต่อกับหน้าประเมินราคาผ่าน Socket.IO (แก้ไขปุ๊บ หน้าประเมินราคาเปลี่ยนปั๊บ)
  - กำหนดรุ่นรถที่รองรับ (Compatible Models)
- **จัดการการจอง**: อนุมัติหรือเลื่อนนัดหมายลูกค้า
- **จัดการผู้ใช้งาน**: ดูแลบัญชีรายชื่อลูกค้าและช่างซ่อม

## 🤖 ระบบ AI Chatbot (AI Architecture)
ระบบแชทบอทถูกออกแบบให้ทำงานแบบ Hybrid โดยใช้ n8n เป็นตัวกลางจัดการ Workflow

1.  **Frontend (ChatWidget)**:
    - ผู้ใช้พิมพ์ข้อความผ่านหน้าเว็บ
    - ส่ง API Request ไปที่ Backend (`/api/chat`)
2.  **Backend Proxy**:
    - รับข้อความจาก Frontend
    - ส่งต่อ (Forward) ไปยัง **n8n Webhook URL**
    - ทำหน้าที่ซ่อน URL จริงของ n8n เพื่อความปลอดภัย
3.  **n8n Workflow**:
    - **Webhook Node**: รับข้อความเข้า
    - **AI Agent Node**: ประมวลผลข้อความโดยใช้ **Groq API** (Model: Llama 3)
    - **System Prompt**: ถูกเทรนให้เป็น "ผู้ช่วยช่าง" มีข้อมูลราคาอะไหล่และเวลาเปิด-ปิดร้าน
    - **Respond to Webhook**: ส่งคำตอบกลับมาที่ Backend -> Frontend

## 🛠 เทคโนโลยีที่ใช้ (Tech Stack)

### Frontend
- **Next.js**: Framework หลักสำหรับการทำ Web App
- **Tailwind CSS**: ตกแต่งหน้าตาเว็บไซต์ให้สวยงามและทันสมัย
- **Lucide React**: ชุดไอคอน
- **Axios**: เชื่อมต่อ API

### Backend & AI
- **Node.js & Express**: สร้าง RESTful API Server
- **Socket.IO**: ระบบสื่อสารแบบ Real-time (ใช้สำหรับอัปเดตข้อมูลอะไหล่ทันที)
- **JWT (JSON Web Token)**: ระบบยืนยันตัวตน (Authentication)
- **Multer**: จัดการการอัปโหลดไฟล์รูปภาพ
- **n8n**: Workflow Automation Tool สำหรับสร้าง Flow การทำงานของ AI
- **Groq API (Llama 3)**: AI Model ที่มีความเร็วสูง ตอบโต้ได้ทันที

### Database
- **PostgreSQL**: ฐานข้อมูลหลักสำหรับเก็บข้อมูลผู้ใช้, การจอง, และสต็อกอะไหล่

## 🚀 การติดตั้งและใช้งาน (Installation)

1. **Clone Project**
   ```bash
   git clone <repository-url>
   cd motorbike_project
   ```

2. **ติดตั้ง Dependencies**
   ```bash
   npm install
   cd frontend && npm install
   cd ..
   ```

3. **ตั้งค่าฐานข้อมูล (Database Setup)**
   - สร้าง Database ใน PostgreSQL
   - รันคำสั่ง SQL จากไฟล์ `backend/config/schema.sql` เพื่อสร้างตารางต่างๆ
   - ตั้งค่า `.env` ใน backend ให้เชื่อมต่อกับ DB

4. **ตั้งค่า AI (n8n Setup)**
   - Import Workflow เข้าไปใน n8n
   - ตั้งค่า Credential ของ Groq API Key
   - นำ Webhook URL มาใส่ใน `.env` ของ Backend (`N8N_WEBHOOK_URL`)

5. **เริ่มต้นระบบ (Run Project)**
   รันคำสั่งเดียวเพื่อเปิดทั้ง Frontend และ Backend พร้อมกัน:
   ```bash
   npm run dev
   ```
   - **Frontend**: เข้าใช้งานได้ที่ `http://localhost:3000`
   - **Backend API**: `http://localhost:5000`

## 📁 โครงสร้างโปรเจกต์
- `frontend/`: Source code ส่วนหน้าบ้าน (Next.js)
- `backend/`: Source code ส่วนหลังบ้าน (API Server)
- `backend/uploads/`: โฟลเดอร์เก็บรูปภาพที่อัปโหลด
